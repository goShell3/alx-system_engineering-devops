# 0x0B. SSH

This directory contains Bash scripts for automating SSH tasks as part of the ALX System Engineering DevOps curriculum.

## Files

- `0-use_a_private_key`: Connects to a server using a private key.
- `1-create_ssh_key_pair`: Generates an RSA SSH key pair named `school`.
